const express = require('express');

const router = express.Router();

router.get('/todos', )
router.post('/todos', )
router.get('/todo/:id', )
router.put('/todo/:id', )
router.delete('/todo/:id', )

module.exports = router